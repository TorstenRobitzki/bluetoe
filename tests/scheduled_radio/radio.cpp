#include "radio.hpp"


std::optional< bluetoe::link_layer::abs_time > radio::receive(
        unsigned channel,
        std::uint32_t access_address,
        bluetoe::link_layer::details::phy_ll_encoding::phy_ll_encoding_t enc,
        bluetoe::link_layer::delta_time timeout )
{
    return {};
}
